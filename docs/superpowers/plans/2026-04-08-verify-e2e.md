# Verify E2E Enhancement — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rewrite verify skill's Step 4 (E2E testing) and rework flow to enable OpenClaw to do maximum-possible end-to-end verification with browser automation, screenshots, and multimodal analysis.

**Architecture:** Single file change to `skills/verify/SKILL.md`. Replace the vague Step 4 with concrete verification flows per project type, and replace the generic rework section with two explicit paths (implementation fix vs requirement clarification).

**Tech Stack:** Markdown (skill definition), references browser automation tools (agent-browser, Puppeteer, curl)

---

## File Structure

| File | Action | Responsibility |
|---|---|---|
| `skills/verify/SKILL.md` | Modify | Replace Step 4 + rework section |

---

### Task 1: Replace Step 4 with E2E verification by project type

**Files:**
- Modify: `skills/verify/SKILL.md:127-136`

- [ ] **Step 1: Replace the existing Step 4 section**

Find lines 127-136 (the current Step 4):

```markdown
### Step 4: E2E 测试（如果可行）

如果项目支持 E2E：
1. 从 spec 的 User Stories 推导出关键用户路径
2. 启动服务
3. 模拟用户操作
4. 验证结果

如果不支持 E2E（CLI 工具、库等）：
- 跳过，在报告中说明
```

Replace with:

```markdown
### Step 4: 端到端验证（最大可能验证）

<HARD-GATE>
E2E 验证不是可选的。对每个项目类型，都必须用最合适的方式做端到端验证。
只有你**真正确认**产出和 spec 对齐了，才能放行。
"看了代码觉得应该没问题" 不算验证。
</HARD-GATE>

#### 4.1 判断项目类型

| 项目类型 | 检测方式 | 验证工具 |
|---|---|---|
| Web 前端/SPA | package.json 有 react/vue/next/vite 等 | 浏览器自动化（agent-browser / Puppeteer） |
| 静态 HTML | index.html 存在，无构建步骤 | 浏览器直接打开文件 |
| API 服务 | 有 routes/endpoints 定义 | curl / HTTP 请求 |
| CLI 工具 | 有 bin 字段或可执行脚本 | 命令行调用 |
| 纯库 | 只有 src，��入口 | 跳过 E2E，但必须在报告中说明依赖 Step 1-2 |

#### 4.2 Web 前端 / 静态 HTML 验证

```
1. 启动
   - 静态文件：浏览器打开 file://<absolute-path>
   - 需要构建：npm run build && npx serve dist
   - 需要 dev server：npm run dev

2. 截图初始状态
   - 截图保存到 /tmp/verify-screenshots/
   - 用多模态模型分析截图，对照 spec 的视觉要求逐条确认
   - 示例 prompt："这是一个 Mario 游戏的初始画面吗？
     能看到：(1) 马里奥精灵 (2) 地面 (3) 问号砖块 (4) 管道？"

3. 模拟用户交互
   - 从 spec 的功能列表推导出关键交互路径
   - 每个交互：操作 → 等待 → 截图 → 多模态分析
   - 示例：按右方向键 → 等 1 秒 → 截图 → "马里奥有没有向右移动？"

4. 检查控制台
   - 捕获所有 console.error 和 JS 异常
   - 任何未捕获异常 = 标记为问题

5. 汇总
   - 所有截图路径 + 多模态分析结果 → 写入 E2E 验证报告
```

#### 4.3 API 服务验证

```
1. 启动服务（从 plan 或项目配置获取启动命令）
2. 对照 spec 的 API 列表，逐个端点验证：
   - 正常请求 → 检查状态码 + 响应体结构
   - 边界输入 → 检查错误处理
   - 认证（如果有）→ 检查无 token 时拒绝
3. 停止服务
```

#### 4.4 CLI 工具验证

```
1. 对照 spec 的命令列表，逐个验证：
   - 正常输入 → 检查输出 + exit code
   - 无参数 / 错误参数 → 检查 help 输出和错误提示
   - 边界输入 → 检查不崩溃
2. 如果 CLI 有文件输出 → 检查输出文件内容
```
```

- [ ] **Step 2: Verify the markdown structure is consistent**

Read the full file after edit, check section numbering and heading levels are consistent with Step 1/2/3/5.

- [ ] **Step 3: Commit**

```bash
git add skills/verify/SKILL.md
git commit -m "feat(verify): rewrite Step 4 with concrete E2E verification by project type"
```

---

### Task 2: Replace the rework flow with two explicit paths

**Files:**
- Modify: `skills/verify/SKILL.md:191-229`

- [ ] **Step 1: Replace the "处理不通过" section**

Find lines 191-229 (the current rework section). Replace the entire section from `## 处理不通过` through the end of `### 严重问题` with:

```markdown
## 处理不通过

### 路径 A：需求与实现不一致 → CC 修

当 OpenClaw 能明确判断"spec 说的是 X，但实现做的是 Y"时，直接让 CC 修。

```
cc-delegate session continue --name superclaw-<feature> --prompt "
验收发现以下问题，请使用 superpowers:systematic-debugging 修复：

## 问题 1
[截图路径（如果有）]
预期：spec 说 XXX
实际：截图/测试显示 YYY

## 问题 2
...

修复后跑测试确认。
"
```

**关键规则：**
- 使用 `session continue` 复用同一个 session
- 附截图让 CC 看到问题（如果是视觉问题）
- 必须指定用 `superpowers:systematic-debugging`，不让 CC 自由发挥
- CC 修完后 → **完整重新走 verify**（不只检查修复的部分）
- 每次返工都要在验证报告里记录（第几次返工、修了什么）

### 路径 B：需求本身有问题 → 找人

当 OpenClaw 不确定是实现错了还是 spec 不够清晰时，**不猜，找人**。

通知用户（飞书）：
```
⚠️ SuperClaw 验收发现问题，需要你判断：

[截图（如果有）]
spec 说：'...'
实际效果：'...'
我不确定是实现错了还是需求不够清晰。

请判断：
1. 实现是对的，继续验收
2. 实现有问题，我让 CC 改
3. spec 需要补充，回 align 阶段
```

**判断标准：**
- spec 里有明确的 acceptance criteria 且实现不满足 → 路径 A（CC 修）
- spec 的描述模糊、可以有多种理解 → 路径 B（找人）
- 实现了 spec 没提到的东西 → 路径 A（让 CC 删掉多余的）
- 发现 spec 遗漏了重要场景 → 路径 B（补充需求）
```

- [ ] **Step 2: Update the "流程" section to reflect the two paths**

Find the flow diagram (lines 59-75) and replace:

```
          ├─ 可自动修 → 发修复指令给 CC → CC 修 → 重新 verify（循环）
          ├─ 需要人工决定 → 通知用户 → 用户决定
          └─ 严重问题 → 通知用户，建议回 plan 重新规划
```

With:

```
          ├─ 需求与实现不一致 → 路径 A：session continue + systematic-debugging → CC 修 → 完整重新 verify
          └─ 需求本身有问题 → 路径 B：飞书通知用户 → 等用户判断
```

- [ ] **Step 3: Update the "终态" section**

Find the 终态 section (lines 257-261) and replace with:

```markdown
## 终态

- **PASS / PASS_WITH_NOTES** → invoke `superclaw:deliver`
- **FAIL（路径 A）** → session continue + systematic-debugging → CC 修 → 完整重新 verify（循环）
- **FAIL（路径 B）** → 飞书通知用户，等用户决定（继续 / CC 改 / 回 align）
```

- [ ] **Step 4: Update Anti-Patterns**

Add to the Anti-Pattern section:

```markdown
❌ 看了代码觉得应该没问题就放行 — 必须实际验证（跑测试/截图/调用）
❌ 让 CC 自由发挥修 bug — 必须指定 superpowers:systematic-debugging
❌ 不确定是 spec 还是实现的问题时猜 — 找人
❌ 返工后只检查修复的部分 — 每次都完整 verify
❌ E2E 验证时不截图 — 截图是证据，写进报告
```

- [ ] **Step 5: Commit**

```bash
git add skills/verify/SKILL.md
git commit -m "feat(verify): two-path rework flow (CC fix vs ask human)"
```

---

### Task 3: Update verification report format

**Files:**
- Modify: `skills/verify/SKILL.md:147-189`

- [ ] **Step 1: Add E2E section to the report template**

Find the report format (lines 147-189) and add after `E2E 测试：XX/XX 通过（或 N/A）`:

```markdown
## E2E 验证详情
- 项目类型：[Web 前端 / API / CLI / 纯库]
- 验证工具：[agent-browser / curl / CLI]
- 截图数量：N 张（保存在 /tmp/verify-screenshots/）

### 截图验证
| # | 操作 | 截图 | 多模态分析结果 | 判定 |
|---|---|---|---|---|
| 1 | 打开初始页面 | screenshot-01.png | 看到马里奥、地面、砖块 | ✅ |
| 2 | 按右方向键 | screenshot-02.png | 马里奥向右移动了约 3 格 | ✅ |
| 3 | 按跳跃键 | screenshot-03.png | 马里奥未跳起 | ❌ |

### 控制台检查
- JS 异常：0 / 1 / N 个
- 详情：...

### 返工记录（如果有）
| 次数 | 问题 | 路径 | 结果 |
|---|---|---|---|
| 1 | 跳跃不工作 | A（CC 修） | 修复后通过 |
```

- [ ] **Step 2: Commit**

```bash
git add skills/verify/SKILL.md
git commit -m "feat(verify): add E2E detail and rework tracking to report format"
```

---

### Task 4: Install and verify

- [ ] **Step 1: Run install to update OpenClaw skills**

```bash
bash scripts/install.sh --skip-hooks --skip-cc-delegate
```

Verify: `Linked: verify` appears in output.

- [ ] **Step 2: Verify the symlink points to updated file**

```bash
grep "HARD-GATE" /root/.openclaw/workspace/skills/superclaw/references/verify.md
```

Expected: Should find the new HARD-GATE in Step 4.

- [ ] **Step 3: Push**

```bash
git push origin main
```
